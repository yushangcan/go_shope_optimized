# Grafana dashboard placeholder

Prometheus is wired into `compose.optimized.yaml`. Import a dashboard built from
`seckill_http_requests_total`, `seckill_http_request_duration_seconds`,
`seckill_admissions_total`, and `seckill_worker_events_total` after the first
real load-test run. No performance numbers are committed in this repository.
